package com.example.idiomatic.gradle.javarca.model;

/**
 * Define the stage in which a Game takes place via symbols representing the {@link Actor}s to be initially placed in
 * the game. A stage needs to have 16 lines with each line being 16 characters long.
 */
public interface Stage {
    String define();
}
